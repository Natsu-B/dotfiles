# This is a placeholder for your Hyprland configuration.
# Please replace the contents of this file with your actual settings.

# Autostart xremap for the Hyprland session
# Make sure this line remains in your configuration.
exec-once = systemctl --user start xremap-hypr.service

# --- Example Settings (replace with your own) ---

# Monitor configuration
monitor=,preferred,auto,1

# Keybindings
bind = SUPER, Q, killactive,
bind = SUPER, M, exit,

# Other settings...
# source = ~/.config/hyprland/my-settings.conf
